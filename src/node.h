#ifndef NODE_H
#define NODE_H

#include "SharedBuffer.h"

#include <vector>

// Draft of the Node class

template<typename T>
class Node
{
public:
private:
    //SharedBuffer<T> buffer;
};

#endif // NODE_H
